public static class SoundHelper
{
    public enum ESoundType
    {
        BGM,
        EffectUI,
        EffectGame
    }

    public enum EName
    {
        BGM_Start = 1,

        EffectUI_ButtonClick = 1000,
    }

    public const float DEFAULT_VOLUME = 1f;
}
